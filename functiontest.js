function redirect() {
      window.location.replace("https://gladyslacia.github.io");
    }

    // Check if the URL is non-existent and redirect if needed
    if (window.location.href === "https://gldirect.github.io/document") {
      redirect();
    }
